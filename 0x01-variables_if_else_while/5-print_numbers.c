#include <stdio.h>

/**
 * main - Prints all strings from 0 to 9
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i++;)
	{
		printf("0123456789", i);
	}
	putchar('\n');
	return (0);
}
